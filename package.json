{
  "name": "minfactory",
  "private": true,
  "scripts": {
    "all:client": "npm --prefix client run all",
    "all:server": "npm --prefix server run all",
    "format:client": "npm --prefix client run format",
    "format:server": "npm --prefix server run format",
    "start:client": "npm --prefix client run start",
    "start:server": "npm --prefix server run start"
  }
}
